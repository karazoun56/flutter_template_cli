import 'package:flutter/material.dart';

class LoginScreenCompact extends StatelessWidget {
  const LoginScreenCompact({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
